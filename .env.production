# Laravel Production Environment Configuration
# For Coolify Deployment

# --- App ---
APP_NAME="Jua"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://jua.twunhu.app
APP_TIMEZONE=UTC

# If behind a load balancer/proxy (e.g., Cloudflare, ELB), set trusted proxies appropriately.
TRUSTED_PROXIES=*

# --- Logging ---
LOG_CHANNEL=stack
LOG_LEVEL=error

# --- Database ---
DB_CONNECTION=mysql
DB_HOST=rwcoso0sggk8ggsg448w440s
DB_PORT=3306
DB_DATABASE=default
DB_USERNAME=mysql
DB_PASSWORD=TH3gregloyal89

# --- Cache / Session / Queue ---
CACHE_DRIVER=redis
SESSION_DRIVER=redis
QUEUE_CONNECTION=redis

# Redis configuration
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# Session settings
SESSION_LIFETIME=120
SESSION_DOMAIN=jua.twunhu.app
SESSION_SECURE_COOKIE=true

# --- Mail ---
MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=noreply@jua.twunhu.app
MAIL_FROM_NAME="${APP_NAME}"

# --- Filesystems ---
FILESYSTEM_DISK=public
# To use S3 in production:
# FILESYSTEM_DISK=s3
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=
# AWS_USE_PATH_STYLE_ENDPOINT=false
# AWS_URL=
# ASSET_URL=https://cdn.jua.twunhu.app

# --- Broadcasting & Realtime ---
BROADCAST_DRIVER=pusher

# Pusher configuration
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# --- Sanctum / SPA settings ---
SANCTUM_STATEFUL_DOMAINS=jua.twunhu.app
SESSION_DOMAIN=.twunhu.app

# --- Vite ---
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
